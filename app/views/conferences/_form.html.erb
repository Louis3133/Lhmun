<%= form_with(model: conference) do |form| %>
  <% if conference.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(conference.errors.count, "error") %> prohibited this conference from being saved:</h2>

      <ul>
        <% conference.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :date, style: "display: block" %>
    <%= form.datetime_field :date %>
  </div>

  <div>
    <%= form.label :link, style: "display: block" %>
    <%= form.text_area :link %>
  </div>

  <div>
    <%= form.label :locate, style: "display: block" %>
    <%= form.text_field :locate %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
